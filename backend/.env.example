DATABASE_URL=postgresql://user:password@host:5432/liquidacion_db
JWT_SECRET=cambia_esto_por_algo_largo_y_aleatorio
CORS_ORIGIN=http://localhost:5173
PORT=3001
# Railway usually sets NODE_ENV in production. Keep it explicit if needed.
